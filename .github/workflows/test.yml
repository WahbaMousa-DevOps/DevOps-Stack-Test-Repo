name: Test Build

on:
  pull_request:
    branches: [ main ]  

jobs:
  test-build:
    name: CI - Test Build  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Test successful!"
#